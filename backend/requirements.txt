# Core runtime — pure-Python "lite" mode works with only these.
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2
python-multipart>=0.0.9

# Custom learned risk model (app.ml) — NumPy is all the trained model needs to
# serve. Without it the engine still runs and falls back to the rule scorer.
numpy>=1.26
